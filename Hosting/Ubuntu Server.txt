#For windows powershell or command prompt:
type ssh root@ubuntu_IP_address in the terminal
Enter Ubuntu Password

#For PuTTY:
Enter ubuntu_IP_address in the HOST_NAME/IP_ADDRESS field
and press Open
type root in the terminal
Enter Ubuntu Password

ufw status (helps us see the current state of Uncomplicated Firewall in linux)

apt install apache2

ufw app list

ufw app info "Apache Full"

ufw allow in "Apache Full"

cd /var/www/

cd html

ls html (gives index.html file)

vim index.html (to edit the file)

wget web_address_of_an_image (converts link of image to image_file.jpg)

mv image_file.jpg image.jpg (to rename the file)

#in the browser type the ubuntu_IP_address in the search bar to see the static index.html page